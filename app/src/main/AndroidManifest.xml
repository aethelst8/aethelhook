<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AethelHook"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AethelHook"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Full 5-option approval screen opened by tapping the notification body -->
        <activity
            android:name=".ApprovalActivity"
            android:exported="false"
            android:theme="@style/Theme.AethelHook"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask" />

        <!-- Multi-choice question screen for AskUserQuestion, opened by tapping the notification body -->
        <activity
            android:name=".QuestionActivity"
            android:exported="false"
            android:theme="@style/Theme.AethelHook"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask" />

        <!-- Plan-mode "Accept this plan?" review screen, opened by tapping the notification body -->
        <activity
            android:name=".PlanReviewActivity"
            android:exported="false"
            android:theme="@style/Theme.AethelHook"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask" />

        <!-- zxing-android-embedded's own manifest defaults this to landscape; override to portrait -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="portrait"
            tools:replace="android:screenOrientation" />

        <!-- Foreground service: keeps LAN WebSocket alive when app is backgrounded -->
        <service
            android:name=".AethelHookWebSocketService"
            android:exported="false"
            android:foregroundServiceType="remoteMessaging" />

        <!-- Starts the foreground service on phone reboot if gateway is enabled -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".DecisionBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <!-- Legacy -->
                <action android:name="com.aethelhook.app.ACTION_APPROVE" />
                <action android:name="com.aethelhook.app.ACTION_DECLINE" />
                <!-- New granular actions -->
                <action android:name="com.aethelhook.app.ACTION_ALLOW_ONCE" />
                <action android:name="com.aethelhook.app.ACTION_ALWAYS_ALLOW_PROJECT" />
                <action android:name="com.aethelhook.app.ACTION_ALWAYS_ALLOW_GLOBAL" />
                <action android:name="com.aethelhook.app.ACTION_DENY" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".TransferDecisionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.aethelhook.app.ACTION_TRANSFER_APPROVE" />
                <action android:name="com.aethelhook.app.ACTION_TRANSFER_DENY" />
            </intent-filter>
        </receiver>
    </application>

</manifest>