<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- AethelHook only ever talks to the local PC — cleartext is fine for LAN -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
